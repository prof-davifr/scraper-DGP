# CSVs gerados (contêm PII: nomes/contatos) — nunca versionar
/*.csv

# TXTs gerados — exceto a lista de grupos (só ID + nome, versionada)
/*.txt
!lista de grupos de pesquisa.txt

# Arquivos de exemplo (rastreados explicitamente)
!samples/

# Lock files do LibreOffice
.~lock.*

# Dependências Node
node_modules/

# Credenciais SUAP — nunca versionar
.env
suap/.env
